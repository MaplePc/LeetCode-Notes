 public int removeElement(int[] nums, int val) {
        int len = nums.length;
        if(len <= 0)
            return 0;
        int delta = 0;  //利用增量的概念, 假设一开始所有的元素已经处于恰当的位置; 以后每检测到一个值为val的元素, delta++
        for(int i = 0; i < len; i++){
            if(nums[i] == val){
                delta++;
            }
            else if(delta != 0)
                nums[i - delta] = nums[i]; //通过i-delta得到元素的正确位置
        }
        return len - delta;
    }
